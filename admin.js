const {C,$,esc,getDemoUser,getSupabase,requireDemo}=Common;requireDemo();let members=[],report={},supa=null;async function load(){if(C.DEMO_MODE){if(getDemoUser()?.role!=='admin')return location.href='members.html';members=DEMO_MEMBERS.map((m,i)=>({...m,last_seen_at:i<12?new Date(Date.now()-i*43200000).toISOString():null}));report={total:members.length,active:members.filter(x=>x.status==='active').length,blocked:members.filter(x=>x.status==='blocked').length,messages_7d:DEMO_MESSAGES.length}}else{supa=await getSupabase();const {data:{user}}=await supa.auth.getUser();if(!user)return location.href='index.html';const {data:p}=await supa.from('profiles').select('role').eq('id',user.id).single();if(p?.role!=='admin')return location.href='members.html';let r=await supa.from('profiles').select('id,full_name,username,status,headline,bio,building,can_help,looking_for,skills,last_seen_at').order('full_name');if(r.error)throw r.error;members=r.data||[];report=await callAdmin('report_summary')}renderStats();renderRows()}
function completeness(m){return Math.round(([m.headline,m.bio,m.building,m.can_help,m.looking_for,(m.skills||[]).length].filter(Boolean).length/6)*100)}function renderStats(){const items=[['Total members',report.total],['Active',report.active],['Blocked',report.blocked],['Messages · 7 days',report.messages_7d]];$('#stats').innerHTML=items.map(([a,b])=>`<div class="stat"><strong>${esc(b??0)}</strong><span>${esc(a)}</span></div>`).join('')}
function renderRows(){const q=$('#adminSearch').value.toLowerCase();$('#rows').innerHTML=members.filter(m=>[m.full_name,m.username,m.location,...(m.skills||[])].join(' ').toLowerCase().includes(q)).map(m=>`<tr><td><b>${esc(m.full_name)}</b></td><td>${esc(m.username)}</td><td><span class="status ${m.status}">${esc(m.status)}</span></td><td>${completeness(m)}%</td><td>${m.last_seen_at?new Date(m.last_seen_at).toLocaleDateString():'—'}</td><td><button class="btn" onclick="toggleBlock('${m.id}')">${m.status==='blocked'?'Unblock':'Block'}</button> <button class="btn" onclick="resetPass('${m.id}','${esc(m.full_name)}')">Reset password</button></td></tr>`).join('')}
async function callAdmin(action,payload={}){const {data:{session}}=await supa.auth.getSession();const res=await fetch(`${C.SUPABASE_URL}/functions/v1/admin-user`,{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${session.access_token}`},body:JSON.stringify({action,...payload})});const out=await res.json();if(!res.ok)throw new Error(out.error||'Admin action failed');return out}
window.toggleBlock=async id=>{const m=members.find(x=>x.id===id);if(C.DEMO_MODE){m.status=m.status==='blocked'?'active':'blocked'}else await callAdmin(m.status==='blocked'?'unblock_user':'block_user',{user_id:id});m.status=m.status==='blocked'?'active':'blocked';renderRows();report.active=members.filter(x=>x.status==='active').length;report.blocked=members.filter(x=>x.status==='blocked').length;renderStats()}
window.resetPass=async(id,name)=>{let pwd;if(C.DEMO_MODE)pwd='Preview-Only';else pwd=(await callAdmin('reset_password',{user_id:id})).temporary_password;alert(`Temporary password for ${name}:\n\n${pwd}\n\nSend this privately. The user should change it after login.`)};$('#adminSearch').addEventListener('input',renderRows);$('#addUser').onclick=()=>alert('For production, provision new members through the secure admin provisioning function or the bulk import script.');load().catch(e=>alert(e.message));

$('#exportReport').onclick=()=>{const cols=['Name','Username','Status','Profile completeness','Location','Topics','Last seen'];const rows=members.map(m=>[m.full_name,m.username,m.status,completeness(m)+'%',m.location||'',(m.skills||[]).join('; '),m.last_seen_at?new Date(m.last_seen_at).toISOString():'']);const csv=[cols,...rows].map(r=>r.map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(',')).join('\n');const blob=new Blob([csv],{type:'text/csv;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='SE_Connect_member_report.csv';a.click();URL.revokeObjectURL(a.href)};
